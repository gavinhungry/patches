diff -ru aur/slimlock.ORIG/src/slim-1.3.6/slimlock.cpp aur/slimlock/src/slim-1.3.6/slimlock.cpp
--- aur/slimlock.ORIG/src/slim-1.3.6/slimlock.cpp	2018-09-06 01:36:19.544228827 -0700
+++ aur/slimlock/src/slim-1.3.6/slimlock.cpp	2018-09-06 02:05:03.292331041 -0700
@@ -167,12 +167,20 @@
 	XMapWindow(dpy, win);
 
 	XFlush(dpy);
-	for (int len = 1000; len; len--) {
-		if(XGrabKeyboard(dpy, root, True, GrabModeAsync, GrabModeAsync, CurrentTime)
-			== GrabSuccess)
-			break;
+
+	int tries = 0;
+ 	while (XGrabKeyboard(dpy, root, True, GrabModeAsync, GrabModeAsync, CurrentTime)
+		!= GrabSuccess) {
+
+		if (tries > 1000) {
+			cerr << "Could not grab keyboard!\n";
+			exit(EXIT_FAILURE);
+		}
+
+		tries++;
 		usleep(1000);
 	}
+
 	XSelectInput(dpy, win, ExposureMask | KeyPressMask);
 
 	// This hides the cursor if the user has that option enabled in their
