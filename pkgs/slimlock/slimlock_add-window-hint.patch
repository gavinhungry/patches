diff -ru aur/slimlock.ORIG/src/slim-1.3.6/slimlock.cpp aur/slimlock/src/slim-1.3.6/slimlock.cpp
--- aur/slimlock.ORIG/src/slim-1.3.6/slimlock.cpp	2018-06-13 01:18:07.183138784 -0700
+++ aur/slimlock/src/slim-1.3.6/slimlock.cpp	2018-06-13 01:18:07.183138784 -0700
@@ -166,6 +166,16 @@
 	  &wa);
 	XMapWindow(dpy, win);
 
+	XClassHint* classHint;
+	classHint = XAllocClassHint();
+	if (classHint) {
+		classHint->res_name = APPNAME;
+		classHint->res_class = "Slimlock";
+		XSetClassHint(dpy, win, classHint);
+ 	}
+
+	XFree(classHint);
+
 	XFlush(dpy);
 	for (int len = 1000; len; len--) {
 		if(XGrabKeyboard(dpy, root, True, GrabModeAsync, GrabModeAsync, CurrentTime)
